# Create .env.example
cat > chat-app/backend/.env.example << 'EOF'
NODE_ENV=production
PORT=3000
MONGODB_URI=mongodb://localhost:27017/chat-app
JWT_SECRET=your-jwt-secret-key-here
EOF

# Create .env for production (with safe defaults)
cat > chat-app/backend/.env << 'EOF'
NODE_ENV=production
PORT=10000
JWT_SECRET=render-production-secret-key-2024
EOF
